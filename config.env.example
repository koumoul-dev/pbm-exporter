# Configuration pour pbm-exporter
# Ce fichier est utilisé par systemd pour configurer l'exporteur

# URI de connexion MongoDB (OBLIGATOIRE)
# Exemples:
#   Simple: mongodb://localhost:27017
#   Avec auth: mongodb://username:password@localhost:27017/admin
#   Replica set: mongodb://host1:27017,host2:27017/admin?replicaSet=myReplSet
#   Avec TLS: mongodb://host:27017/admin?ssl=true&sslCAFile=/path/to/ca.pem
PBM_MONGODB_URI=mongodb://localhost:27017

# Port d'écoute du serveur (optionnel, défaut: 9090)
PORT=9090

# Activer les logs de debug (optionnel, défaut: false)
# DEBUG=1

# Autres options possibles:
# GOMAXPROCS=2  # Limiter le nombre de processeurs utilisés
